# DraftToBlog 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | DraftToBlog |
| 产品定位 | PDF、Word、Markdown 一键转公开博客内容工具 |
| 产品形态 | 部署于云服务器的 Web 应用 |
| 技术栈 | React + Python FastAPI |
| 图床方案 | PicGo + 腾讯云 COS |
| 目标赛道 | 学习工作 |
| 当前阶段 | 初赛可体验 Demo |
| 文档版本 | v1.0 |
| 需求来源 | `registration-post.md`、`competition-requirements.md` |

## 2. 产品概述

### 2.1 背景

开发者、产品经理、运营人员和内容创作者经常在飞书中记录项目复盘、问题排查、学习笔记和工作总结。这些内容已经具备知识价值，但从内部草稿到公开博客之间仍有大量重复劳动：

- 图片引用通常是本地路径，发布后容易裂图；
- 原始记录结构松散，不适合公开阅读；
- 文档可能包含公司、项目、人名、账号、联系方式、内网地址和业务数据等敏感信息；
- 图片上传、链接替换、内容整理、隐私检查与网页生成需要在多个工具间反复切换。

此外，飞书导出的 Markdown 可能不包含图片资源，只保留失效引用或完全缺少图片。DraftToBlog 需要识别这一情况，引导用户补充图片，或优先导入包含内嵌图片的 Word/PDF 原文件。

DraftToBlog 将上述流程收敛为一次导入、一次检查和一次导出，让 PDF、Word、Markdown 等工作文档更安全、高效地转化为可公开传播的内容资产。

### 2.2 一句话定义

用户导入 PDF、Word 或 Markdown 文档后，DraftToBlog 自动提取并上传图片、优化文章结构、识别并脱敏隐私信息，最终生成可发布的 Markdown、Word 或 PDF。

### 2.3 核心价值

1. **效率价值：** 将原本约 10–30 分钟的手工整理压缩为一次自动处理流程。
2. **内容价值：** 把流水记录转化为结构清晰、具备教学和传播价值的公开文章。
3. **安全价值：** 在发布前发现敏感信息，并通过逐项确认和脱敏日志降低遗漏风险。
4. **可控价值：** 保留原文、修改对比和人工确认，用户始终拥有最终发布决定权。

## 3. 产品目标与边界

### 3.1 初赛 Demo 目标

- 跑通“导入 → 解析 → 图片处理 → 内容优化 → 隐私脱敏 → 预览确认 → 导出”的完整核心链路；
- 让评审在 3 分钟内理解目标用户、真实痛点与产品价值；
- 将应用部署到云服务器并提供可公开访问的体验地址；
- 用真实示例证明 DraftToBlog 已能处理 PDF、Word 与 Markdown，而不是概念展示；
- 当前版本聚焦产品开发；报名帖链接、体验链接与 TRAE Session ID 待产品完成后统一补充。

### 3.2 成功标准

| 指标 | 初赛 Demo 目标 |
| --- | --- |
| 核心任务完成率 | 测试用户中 ≥ 90% 能独立完成一次转换 |
| 首次可用结果时间 | 典型文档从导入到预览 ≤ 60 秒，不含第三方接口异常 |
| 图片识别率 | Word/PDF 内嵌图片和 Markdown 有效图片引用达到 100% 识别 |
| 敏感信息提示 | 内置规则覆盖手机号、邮箱、身份证号、IPv4、URL/内网地址等常见类型 |
| 导出可用性 | 导出的 Markdown、Word 和 PDF 可正常打开，图片无裂图 |
| 严重错误 | 核心演示路径无阻断性错误 |

### 3.3 非目标

以下内容不纳入初赛 MVP：

- 多人实时协作、团队权限和审批流；
- 完整 CMS、博客托管平台或社区能力；
- 自动发布到所有第三方博客平台；
- 复杂排版编辑器和主题市场；
- 对敏感信息“零遗漏”的承诺；
- 企业级审计、计费和组织管理。

## 4. 用户与场景

### 4.1 核心用户

| 用户 | 典型内容 | 核心诉求 |
| --- | --- | --- |
| 开发者 | 故障排查、技术方案、项目复盘 | 修复图片、隐藏内网信息并生成技术博客 |
| 产品经理 | 需求分析、上线复盘、调研记录 | 重构文章逻辑并隐藏项目与业务信息 |
| 运营人员 | 活动复盘、流程总结、数据分析 | 快速整理结构并脱敏业务数据 |
| 技术写作者 | 教程草稿、学习笔记 | 降低排版和静态网页生成成本 |
| 知识管理用户 | 飞书中的长期个人记录 | 将内部知识沉淀为可发布内容资产 |

### 4.2 核心场景

1. 用户完成一次线上故障排查，希望将记录整理成公开教程。
2. 用户完成项目复盘，希望删除内部名称和数据后发布到个人博客。
3. 用户在飞书积累学习笔记，希望修复图片并生成结构完整的 HTML 页面。
4. 用户希望在发布前获得统一的隐私风险检查，减少人工逐行排查。

### 4.3 关键用户故事

- 作为开发者，我希望系统自动识别本地图片并替换为可访问链接，以免发布后出现裂图。
- 作为内容创作者，我希望系统在不改变事实的前提下优化文章结构，让草稿更适合公开阅读。
- 作为职场用户，我希望在导出前看到所有敏感信息及替换结果，避免误发内部信息。
- 作为谨慎的用户，我希望逐项接受、修改或忽略系统建议，而不是让 AI 直接覆盖原文。
- 作为博客作者，我希望按需获得 Markdown、Word 或 PDF，便于发布、归档和继续编辑。

## 5. 产品原则

1. **隐私优先：** 云端处理遵循最小留存原则，任务文件到期自动清理。
2. **先提示后处理：** 调用 AI 或 PicGo/COS 前清晰说明数据去向，并由用户主动确认。
3. **修改可追溯：** 原文不被直接覆盖，所有优化和脱敏都可对比、撤销。
4. **安全不替用户做决定：** 系统提示风险，最终发布由用户确认。
5. **先跑通核心链路：** 初赛阶段优先保证转换结果真实可用，不堆砌低价值功能。

## 6. 信息架构

### 6.1 页面结构

1. **首页/导入页**
   - 产品价值说明；
   - PDF、Word、Markdown 或示例文档入口；
   - 隐私处理说明。
2. **处理工作台**
   - 左侧：流程步骤与处理进度；
   - 中间：原文/结果编辑与预览；
   - 右侧：图片、内容优化、敏感项和日志面板。
3. **导出页**
   - 转换摘要；
   - Markdown、Word、PDF 下载；
   - 发布前检查清单。

### 6.2 核心流程

1. 用户导入 `.pdf`、`.docx`、`.md`，或选择内置示例。
2. 系统解析标题、正文结构、图片引用与资源文件。
3. 系统显示预检结果，并让用户选择图片处理方式和是否启用 AI 优化。
4. 系统执行图片上传/内嵌、结构优化和敏感信息扫描。
5. 用户在对比视图中逐项确认图片、内容和脱敏建议。
6. 系统执行发布前检查并按用户选择生成 Markdown、Word 或 PDF。
7. 用户下载结果并在目标平台发布。

## 7. 功能需求

### 7.1 P0：文档导入与解析

#### 功能说明

- 支持拖拽或选择 `.pdf`、`.docx`、`.md` 文件；
- Word 首版支持 `.docx`，不支持旧版 `.doc`；
- PDF 首版支持可提取文字的标准 PDF；扫描版 PDF 给出提示，OCR 作为后续能力；
- 提供一份内置示例，无需配置即可体验核心流程；
- 统一提取标题层级、正文、列表、表格、链接和图片；
- 对 Markdown 额外解析 YAML Front Matter、代码块，以及相对路径、绝对路径、URL 和 Base64 图片；
- 提取 Word/PDF 中的内嵌图片，并保留其在正文中的相对位置；
- 当飞书 Markdown 不含图片资源时，明确提示“源文件未携带图片”，支持用户补充上传图片并手动关联位置，或改用 Word/PDF 原文件；
- 导入后显示文件名、字数、图片数和潜在问题数。

#### 验收标准

- 合法 PDF、DOCX 与 UTF-8 Markdown 能成功载入且正文不丢失；
- DOCX/PDF 内嵌图片可被提取，Markdown 可访问图片可建立正确引用；
- 不支持的文件、空文件、加密文件、损坏文件和编码异常有明确提示；
- 缺少图片的 Markdown 不会被误判为处理成功，系统必须展示补图建议；
- 代码块内类似图片或敏感信息的文本不被错误解析为正文引用。

### 7.2 P0：图片处理与链接替换

#### 功能说明

- 列出所有图片及其状态：已提取、可用、缺失、远程链接、待上传、失败；
- 对 Word/PDF 内嵌图片和 Markdown 可访问图片生成预览；
- 首版采用 PicGo + 腾讯云 COS 作为图床方案；
- FastAPI 后端调用 PicGo 完成上传，PicGo 使用腾讯云 COS 配置；
- PicGo/COS 配置由服务器环境变量提供，不向浏览器暴露腾讯云密钥；
- 上传成功后自动替换 Markdown 图片地址；
- 上传失败时支持重试、跳过和保留原路径；
- 记录原路径、新地址、处理状态和失败原因。

#### 验收标准

- 所有被替换的图片链接在预览中可打开；
- 同一图片重复引用时不重复上传；
- 缺失图片不会静默跳过，导出前必须提示；
- 未经用户确认，不向腾讯云 COS 上传任何资源；
- 官方测试图床应使用独立 Bucket/目录、限制文件类型与大小，并配置自动清理策略。

### 7.3 P0：内容结构优化

#### 功能说明

- 分析原文标题层级、段落长度、上下文缺失和重复内容；
- 提供结构优化建议，包括引言、章节重组、步骤化说明和总结；
- 支持“仅整理结构”和“结构 + 表达优化”两档；
- 不自动编造数据、结论、经历或技术事实；
- 以差异对比展示删除、增加和改写内容；
- 支持全部接受、逐项接受、编辑建议、撤销与恢复原文。

#### 验收标准

- 原始文档始终可恢复；
- 未确认建议不进入最终导出内容；
- 代码块、命令、URL 和专有名词默认保持原样；
- 优化结果保持核心事实和原始意图一致。

### 7.4 P0：隐私识别与脱敏

#### 功能说明

- 使用本地规则识别手机号、邮箱、身份证号、IP 地址、内网 URL、账号等常见信息；
- 支持用户添加公司名、项目代号、内部系统、人名和自定义关键词；
- 对不确定的上下文敏感信息提供 AI 辅助识别选项；
- 每个命中项显示类型、原文片段、所在位置、风险等级和建议替换值；
- 支持掩码、泛化、占位符、自定义替换、忽略；
- 生成脱敏日志，但日志默认不完整保存敏感原值；
- 导出前再次扫描未处理的高风险项。

#### 默认替换示例

| 类型 | 原值示例 | 默认结果示例 |
| --- | --- | --- |
| 手机号 | 13812345678 | 138****5678 |
| 邮箱 | user@example.com | u***@example.com |
| IP 地址 | 10.20.30.40 | `[内网 IP]` |
| 公司/项目 | 星河公司/Apollo | `[公司名称]` / `[项目代号]` |
| 人名 | 张三 | `[相关人员]` |
| 业务数据 | 日活 123456 | `[业务数据]` |

#### 验收标准

- 用户能定位每个命中项并查看上下文；
- 用户可逐项确认或忽略，不发生不可见的自动替换；
- 导出前存在未处理高风险项时给出阻断式二次确认；
- 页面明确声明自动识别无法保证零遗漏。

### 7.5 P0：预览与差异确认

#### 功能说明

- 提供原文、差异和最终预览三种视图；
- 支持桌面与移动端文章宽度预览；
- Markdown 编辑与 HTML 预览联动；
- 显示目录、图片、代码块、表格和链接的渲染结果；
- 显示处理摘要：替换图片数、优化建议数、脱敏项数和待处理问题数。

#### 验收标准

- 切换视图不丢失用户已确认的修改；
- Markdown 编辑内容与最终渲染预览的标题、正文、图片和代码内容一致；
- 预览中的所有外链可被用户点击检查；
- 待处理阻断项在导出按钮附近清晰展示。

### 7.6 P0：导出与发布前检查

#### 功能说明

- 导出优化后的 `.md`，图片使用腾讯云 COS 的公开 URL；
- 导出可继续编辑的 `.docx`，图片嵌入文档；
- 导出适合归档和分享的 `.pdf`，图片嵌入文档；
- 导出前检查缺失图片、失败链接、未处理高风险项和空标题；
- 生成处理摘要，包含处理数量和时间，不泄露敏感原值。

#### 验收标准

- 导出的 Markdown 使用 UTF-8 编码；
- 导出的 DOCX 可由 Microsoft Word/WPS 正常打开并继续编辑；
- 导出的 PDF 字体、分页、图片和代码块无明显错位；
- 三种格式的标题、正文、图片和脱敏结果保持一致；
- 未通过发布前检查时清楚说明原因和解决入口。

### 7.7 P1：增强能力

- 文章风格模板：技术教程、项目复盘、学习笔记；
- 自定义脱敏词典的导入与导出；
- 一键复制博客 Markdown；
- 更多图床适配器；
- 扫描版 PDF 的 OCR；
- 静态博客主题切换；
- 用户可选的转换历史与自动清理周期；
- 发布到 GitHub Pages、Hexo、Hugo 等目标的适配能力。

## 8. 状态与异常处理

| 场景 | 产品行为 |
| --- | --- |
| 文档编码异常 | 提示编码问题，并允许用户重新选择或转换为 UTF-8 |
| PDF 为扫描件或加密文件 | 说明首版限制，提示解除加密或改用 DOCX/Markdown |
| 飞书 Markdown 未携带图片 | 显示缺失提示，允许补充上传图片并关联，或建议改用 Word/PDF 原文件 |
| 图片文件缺失 | 标记原引用位置，允许补选文件、跳过或保留路径 |
| 图床认证失败 | 停止上传，保留本地结果，提示重新配置 |
| AI 服务不可用 | 保留图片处理与规则脱敏能力，允许跳过内容优化 |
| 用户 API 配置无效 | 不切换到官方 API，先提示用户修正或主动选择官方测试额度 |
| 内容过长 | 分段处理并显示进度，失败段支持单独重试 |
| 浏览器刷新/误关闭 | 在服务器临时保留任务，并在恢复窗口过期后自动清理 |
| 导出存在风险 | 展示问题清单；高风险项需二次确认后才能继续 |

## 9. 数据与技术约束

### 9.1 技术架构

| 层级 | 方案 | 职责 |
| --- | --- | --- |
| 前端 | React | 文件上传、流程工作台、差异确认、预览、明暗主题和配置界面 |
| 后端 | Python FastAPI | 文件解析、任务编排、AI 调用、敏感信息扫描、导出和临时文件管理 |
| 图床 | PicGo + 腾讯云 COS | 图片上传、公开 URL 生成与资源存储 |
| AI | OpenAI-compatible API | 结构优化和可选的上下文敏感信息识别 |
| 部署 | 云服务器 | 托管 React 静态资源与 FastAPI 服务，对外提供 HTTPS 体验地址 |

前后端通过 REST API 通信；耗时处理任务采用任务 ID + 进度轮询，后续可升级为 SSE/WebSocket。生产环境由反向代理统一提供 HTTPS、请求大小限制和访问日志脱敏。

### 9.2 AI API 配置策略

- 系统支持用户填写兼容 OpenAI 协议的 API Base URL、API Key 和模型名称；
- 用户配置优先于官方基础测试 API，仅作用于当前任务，默认不持久化；
- 用户主动选择“使用官方测试额度”时，后端使用服务器 `.env` 中的官方 API 配置；
- 官方 API Key 只存在于服务端，不下发到 React 前端；
- 官方测试能力配置频率、单文档长度和每日额度限制，额度用尽时给出明确提示；
- API 调用前展示数据发送说明，用户可关闭 AI，仅使用基础解析、图片处理和规则脱敏；
- API Key、请求正文和敏感原文不得写入应用日志。

### 9.3 核心数据对象

| 对象 | 关键字段 |
| --- | --- |
| Document | id、文件名、原始内容、工作内容、状态、创建时间 |
| Asset | id、原路径、文件类型、哈希、新地址、上传状态 |
| Suggestion | id、类型、原片段、建议片段、位置、确认状态 |
| SensitiveFinding | id、类型、风险等级、位置、替换策略、处理状态 |
| ExportJob | id、输出格式、检查结果、生成状态、生成时间 |

### 9.4 隐私与安全

- 文档会上传至云服务器处理，上传前明确告知处理范围和自动清理周期；
- 调用 AI 前展示将发送的内容范围，并提供关闭入口；
- 官方 COS 与 AI 密钥仅保存在服务器环境变量中，不进入前端代码；
- 用户填写的 AI API Key 仅在任务期间保存在服务端内存中，默认不落库；
- 日志不得记录完整文档、密钥或敏感原值；
- 处理完成后提供“一键清除任务数据”，临时文件到期自动删除；
- HTML 渲染必须过滤危险脚本，防止导入内容执行恶意代码；
- 对外宣传不得声称工具能够替代人工安全审查。

### 9.5 性能与兼容性

- MVP 默认支持单文件不超过 20 MB，服务器可通过环境变量调整限制；
- 典型 5,000 字、20 张图片的文档解析应在 3 秒内完成；
- 长任务显示明确进度，不冻结主要界面；
- 支持最新版 Chrome、Edge 和 Safari；
- 关键操作具备键盘可达性，文本和控件满足基本对比度要求。

## 10. 视觉与交互要求

- 采用聚焦工作流的三栏工作台，减少页面跳转；
- 使用稳定一致的状态色：处理中、成功、警告、失败；
- 高风险敏感项不能仅靠颜色表达，必须同时显示图标与文字；
- 主操作在每一步保持唯一明确，避免用户不知道下一步；
- 首次进入提供示例文档，让评审无需准备文件即可体验；
- 支持明亮与暗黑两种主题，并可跟随系统主题；
- 用户手动选择的主题保存于浏览器，下次访问自动恢复；
- 核心价值在首页、处理摘要和导出结果中持续可见；
- 动效只用于进度和状态反馈，避免影响阅读效率。

## 11. 评分标准对应策略

| 评审维度 | 权重 | DraftToBlog 的应对重点 | Demo 证明材料 |
| --- | ---: | --- | --- |
| 创新性 | 30% | 将图片修复、内容重构、隐私脱敏和多格式导出整合为一个可控流程；突出“职场知识公开的最后一公里” | 手工流程与一键流程对比、TRAE 开发过程 |
| 实用性 | 30% | 使用真实飞书导出文档，证明节省时间、降低泄密风险和避免裂图 | 处理前后对比、耗时对比、风险命中结果 |
| 完成度 | 20% | 核心链路完整跑通，失败状态可处理，结果可下载并打开 | 从导入到导出的现场演示 |
| 美观度/设计体验 | 20% | 清晰三栏布局、分步引导、差异确认和明确状态反馈 | 桌面操作录屏、关键界面截图 |

## 12. 初赛 Demo 演示脚本

建议准备一份包含 3 张内嵌图片、结构松散内容以及多类模拟敏感信息的 DOCX 示例，并准备一份不带图片的飞书 Markdown 用于展示异常处理。

1. **痛点展示（20 秒）：** 打开原始 Markdown，展示裂图、流水结构和敏感信息。
2. **导入预检（20 秒）：** 拖入 DOCX，展示字数、图片数和风险数；说明同时支持 PDF、Markdown。
3. **图片处理（30 秒）：** 展示内嵌图片提取、预览、通过 PicGo 上传腾讯云 COS 和链接自动替换。
4. **内容优化（30 秒）：** 展示结构建议和修改差异，人工确认一项建议。
5. **隐私脱敏（40 秒）：** 展示手机号、内网地址与项目代号命中，逐项处理并查看日志。
6. **结果预览（20 秒）：** 展示完整博客页面和移动端宽度效果。
7. **导出验证（20 秒）：** 分别下载 Markdown、Word、PDF，证明正文、图片和脱敏结果一致。
8. **价值总结（20 秒）：** 对比处理耗时、人工步骤和发布风险。

## 13. 参赛交付清单

### 13.1 产品交付

- [ ] 云服务器上的公开体验地址可通过 HTTPS 访问；
- [ ] 内置示例可完整跑通，无需评审配置私有密钥；
- [ ] 核心功能与报名创意方向一致；
- [ ] 主要浏览器完成一次全流程测试；
- [ ] 提交前复查体验链接和导出文件。

### 13.2 作品帖交付

- [ ] 标签使用“学习工作”，与报名赛道一致；
- [ ] 标题包含报名赛道与 Demo 名称；
- [ ] 说明产品形态、目标用户和 2–3 个核心功能；
- [ ] 说明灵感来源、真实痛点和方向取舍；
- [ ] 产品完成后补充体验地址；
- [ ] 产品完成后补充不少于 3 张 TRAE 开发关键步骤截图；
- [ ] 产品完成后补充不少于 3 个关键任务 Session ID；
- [ ] 发帖前补充已通过的社区报名帖链接；
- [ ] 确认内容原创且版权清晰。

## 14. 里程碑建议

| 阶段 | 目标 | 完成标志 |
| --- | --- | --- |
| M1 核心骨架 | 导入、解析、工作台和示例文档 | 示例可载入并正确预览 |
| M2 核心处理 | 图片、内容优化和脱敏 | 三项处理均可确认和撤销 |
| M3 导出闭环 | Markdown、Word、PDF 与预检 | 三种导出文件可独立使用 |
| M4 体验打磨 | 状态、异常、响应式、明暗主题 | 全流程无阻断性问题 |
| M5 云端部署 | React/FastAPI 部署、HTTPS 与环境配置 | 公开地址可稳定完成全流程 |
| M6 参赛材料 | 截图、Session ID、体验链接和作品帖 | 按赛事清单完成自检 |

## 15. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| AI 改写产生事实偏差 | 降低内容可信度 | 差异展示、默认不自动接受、保护代码和专有名词 |
| 敏感信息漏检 | 造成公开发布风险 | 规则 + 自定义词典 + 可选 AI；导出前复扫并声明人工复核责任 |
| PicGo/COS 接口或配置异常 | 核心流程中断 | 上传前配置检测、失败重试、保留任务结果并允许更换配置 |
| 官方测试 API 被滥用 | 成本上升或评审无法体验 | 限流、配额、输入长度限制和服务端密钥保护 |
| 用户 API Key 泄露 | 造成安全与费用风险 | HTTPS、仅服务端内存暂存、日志脱敏、任务结束清除 |
| 公共 Demo 需要密钥 | 评审无法体验 | 提供官方基础测试 API 和内置示例，核心路径不要求评审提供私有密钥 |
| 功能范围过大 | 初赛前无法形成闭环 | 严格优先 P0，P1 不阻塞提交 |
| PDF/Word/Markdown 格式差异 | 解析结果异常 | 格式归一化、预检、测试样例和明确错误提示 |
| 飞书 Markdown 缺少图片 | 输出内容不完整 | 显式告警、补图关联入口，并建议使用含图片的 Word/PDF 原件 |

## 16. 已确认的技术决策与后续事项

### 16.1 已确认

- 图床采用 PicGo + 腾讯云 COS；
- 支持用户自定义 AI API，官方提供基础测试 API；
- 首版导入 PDF、DOCX、Markdown；
- 首版导出 Markdown、DOCX、PDF；
- 前端使用 React，后端使用 Python FastAPI；
- 界面提供明亮与暗黑两种主题；
- 项目最终部署到云服务器。

### 16.2 后续再补充

- 云服务器域名、SSH 地址与部署目录；
- 腾讯云 COS Bucket、Region、访问域名和凭据；
- 官方测试 AI 的 Base URL、模型、Key、额度与限流参数；
- 报名帖链接、体验链接和最终 Session ID 不属于当前开发阶段的阻塞项。

## 17. 文档关系

- 创意报名内容见 [`registration-post.md`](./registration-post.md)；
- 官方赛事原文及评分图片转写见 [`competition-requirements.md`](./competition-requirements.md)；
- 本文档负责将创意与赛事约束转化为产品范围、功能要求、验收标准和参赛交付计划。
